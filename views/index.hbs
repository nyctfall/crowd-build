<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CrowdBuild{{#unless production}} Dev Server{{/unless}}</title>
    {{#if production}}
      {{#each js}}

    <script type="module" src="{{this}}"></script>
    
      {{/each}}
      {{#each css}}

    <link rel="stylesheet" href="{{this}}" />

      {{/each}}
    {{else}}
      {{{VITE_DEV}}}
    {{/if}}
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
  </head>
  <body>
    <div id="root"></div>
  </body>
</html>
